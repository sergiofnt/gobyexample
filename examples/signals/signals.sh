# Як тільки ми запустимо цю программу вона заблокується
# і буде очікувати сигнал. Натискаючи `ctrl-C`
# (який показано тут як `^C`) ми можемо передати сигнал `SIGINT`,
# що змусить програму надрукувати `interrupt` і завершити роботу.
$ go run signals.go
awaiting signal
^C
interrupt
exiting
